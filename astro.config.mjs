// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://hoomexsun.github.io',
    base: '/ehei',
    // site: 'https://ehei.in',
});
